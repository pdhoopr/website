language: "node_js"
node_js:
  - "5.4.1"
before_install:
  - "rvm install 2.3.0"
install:
  - npm install
  - "bundle install --path vendor/bundle"
before_deploy:
  - npm run build
  - "npm install -g surge"
deploy:
  provider: script
  script: "surge ./public"
  on:
    branch: master
