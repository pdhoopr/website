language: "node_js"
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
before_install:
  - "rvm install 2.3.1"
install:
  - npm install
  - "bundle install"
script:
  - npm run lint
  - npm run build
before_deploy:
  - "npm install -g surge"
deploy:
  provider: surge 
  project: "./public"
  domain: "patrick.hoopr.io"
  skip_cleanup: true
  on: 
    branch: production
branches:
  only:
    - master
    - production
