language: "node_js"
node_js:
  - "5.4.1"
before_install:
  - "rvm install 2.3.0"
install:
  - npm install
  - "npm install -g surge"
  - "bundle install --path vendor/bundle"
deploy:
  provider: script
  script: "npm run build && surge ./public"
  skip_cleanup: true
  on:
    branch: master
